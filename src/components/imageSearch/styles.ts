import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  inputContainer: {
    width: '100%',
    padding: 10,
  },
  input: {
    borderWidth: 1,
    height: 50,
    paddingHorizontal: 10,
  },
});
